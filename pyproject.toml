[project]
name = "currency-converter"
version = "0.1.0"
description = "PySide6 currency converter — MVC architecture"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.7.0",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "nuitka>=4.0.8",
    "pillow>=12.2.0",
]
